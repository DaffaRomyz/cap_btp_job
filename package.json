{
  "name": "cap_btp_job-cds",
  "version": "1.0.0",
  "description": "Generated by cds-services-archetype",
  "license": "ISC",
  "repository": "<Add your repository here>",
  "devDependencies": {
    "@sap/cds-dk": "^9.5.0",
    "rimraf": "^5.0.5",
    "mbt": "^1.2.29"
  },
  "sapux": [
    "app/cap_btp_job_account",
    "app/cap_btp_job_transaction",
    "app/cap_btp_job_report"
  ],
  "dependencies": {},
  "cds": {
    "requires": {
      "workzone": true
    }
  },
  "scripts": {
    "undeploy": "cf undeploy cap_btp_job --delete-services --delete-service-keys --delete-service-brokers",
    "build": "rimraf resources mta_archives && mbt build --mtar archive",
    "deploy": "cf deploy mta_archives/archive.mtar --retries 1"
  }
}
